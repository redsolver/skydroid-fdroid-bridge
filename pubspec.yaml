name: skydroid_fdroid_bridge
description: Makes F-Droid apps available via SkyDroid.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  xml2json: ^4.3.0
  yamlicious: ^0.1.0
  yaml: ^2.2.1
  http: ^0.12.2
  crypto: ^2.1.5
  dotenv: ^2.0.0

dev_dependencies:
  pedantic: ^1.9.0
